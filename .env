## OpenStep -- step-ca fuer nuhost6
COMPOSE_PROJECT_NAME=step_ca
COMPOSE_FILE=docker-compose.yml

STEP_CA_TAG=latest
CA_NAME=nuhost6-ca
CA_DNS=step-ca
CA_ACME=true
# CA_PASSWORD=  (leer = auto-generiert, einmalig beim ersten Start angezeigt)
LOG_DRIVER=journald
